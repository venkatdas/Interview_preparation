```js
function simplifySqrt(n) {
  let outside = 1;

  for (let i = 1; i * i <= n; i++) {
    if (n % (i * i) === 0) {
      outside = i;
    }
  }

  let inside = n / (outside * outside);

  if (inside === 1) {
    return `${outside}`;
  }

  return `${outside}√${inside}`;
}
```
